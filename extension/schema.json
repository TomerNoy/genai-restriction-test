{
    "type": "object",
    "properties": {
        "apiDomain": {
            "title": "Domain of the Prompt Security Protect API",
            "description": "Configures the domain of the Protect API by Prompt Security",
            "type": "string"
        },
        "apiKey": {
            "title": "API key for the Prompt Security Protect API",
            "description": "Configures the api key of the Protect API by Prompt Security",
            "type": "string"
        },
        "idp": {
            "title": "SSO IDP - Optional",
            "description": "Configures which identity provider to use for SSO",
            "type": "string"
        },
        "idpUrl": {
            "title": "SSO IDP URL - Optional",
            "description": "The URL of the identity provider to use for SSO",
            "type": "string"
        },
        "idpClientId": {
            "title": "SSO IDP Client ID - Optional",
            "description": "The client id for the identity provider to use for SSO",
            "type": "string"
        },
        "idpClientKey": {
            "title": "SSO IDP Client Key - Optional",
            "description": "The client key for the identity provider to use for SSO",
            "type": "string"
        },
        "addCustomHttpHeader": {
            "title": "Add Custom HTTP Header (true/false) - Optional",
            "description": "Add a custom HTTP header to all requests (to detect incognito usage)",
            "type": "string"
        },
        "corporateDomains": {
            "title": "Corporate Domains - Optional",
            "description": "List of corporate domains",
            "type": "array",
            "items": {
                "type": "string"
            }
        },
        "userName": {
            "title": "User Name - Optional",
            "description": "The username of the end user (can be set via MDM)",
            "type": "string"
        },
        "isSilentSSOEnabled": {
            "title": "Silent SSO Enabled (true/false) - Optional (default: false)",
            "description": "Allow silent SSO when userName is provided (legacy behavior when omitted)",
            "type": "string"
        },
        "machineName": {
            "title": "Machine Name - Optional",
            "description": "The machine name of the end user (can be set via MDM)",
            "type": "string"
        },
        "browserName": {
            "title": "Browser Name - Optional",
            "description": "The browser name of the end user (can be set via MDM)",
            "type": "string"
        }
    }
}